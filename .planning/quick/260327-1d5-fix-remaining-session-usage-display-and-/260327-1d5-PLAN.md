---
phase: quick
plan: 260327-1d5
type: execute
wave: 1
depends_on: [260327-14j]
files_modified:
  - ClaudeTokenMonitor/ClaudeTokenMonitor/Services/UsageWindowTracker.swift
  - ClaudeTokenMonitor/ClaudeTokenMonitor/ClaudeTokenMonitorApp.swift
  - ClaudeTokenMonitor/ClaudeTokenMonitor/Views/DashboardView.swift
  - ClaudeTokenMonitor/ClaudeTokenMonitor/MenubarManager.swift
  - ClaudeTokenMonitor/ClaudeTokenMonitor.xcodeproj/project.pbxproj
autonomous: true
must_haves:
  truths:
    - "UsageWindowTracker is wired into DashboardView via environment object"
    - "PlanUsageBanner uses UsageWindowTracker.currentWindow instead of inline computation"
    - "Remaining session token display is accurate and live-updating"
    - "App builds without errors"
  artifacts:
    - path: "ClaudeTokenMonitor/ClaudeTokenMonitor/Services/UsageWindowTracker.swift"
      provides: "UsageWindowTracker class with @Published currentWindow"
    - path: "ClaudeTokenMonitor/ClaudeTokenMonitor/Views/DashboardView.swift"
      provides: "PlanUsageBanner using UsageWindowTracker"
---

<objective>
Wire UsageWindowTracker into DashboardView so that the PlanUsageBanner displays live remaining-token data from the shared tracker rather than recomputing inline. Fix the remaining session usage display to use the centralized, more accurate window computation.

Purpose: Remove duplicated window-computation logic from the dashboard; use the shared UsageWindowTracker that already has better logic (averaging learned limits from history).
Output: Updated DashboardView.swift and MenubarManager.swift with UsageWindowTracker injected as environment object.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Wire UsageWindowTracker into dashboard via environment object</name>
  <files>
    ClaudeTokenMonitor/ClaudeTokenMonitor/Services/UsageWindowTracker.swift
    ClaudeTokenMonitor/ClaudeTokenMonitor/ClaudeTokenMonitorApp.swift
    ClaudeTokenMonitor/ClaudeTokenMonitor/MenubarManager.swift
    ClaudeTokenMonitor/ClaudeTokenMonitor/Views/DashboardView.swift
    ClaudeTokenMonitor/ClaudeTokenMonitor.xcodeproj/project.pbxproj
  </files>
  <action>
1. In MenubarManager.swift, store the UsageWindowTracker and inject it into the popover content view as an environment object. Add a new method `observeTracker(_ tracker: UsageWindowTracker)` to store it. Update `setupPopover` or create a new setup method that takes the tracker and injects it.

2. In ClaudeTokenMonitorApp.swift AppDelegate, call `manager.observeTracker(tracker)` after `manager.observeBudget(monitor)`.

3. In DashboardView.swift, add `@EnvironmentObject private var usageTracker: UsageWindowTracker` and update PlanUsageBanner to accept a `UsageWindow?` instead of `[TokenRecord]`. Remove the inline computation from PlanUsageBanner.
  </action>
  <verify>
    <automated>DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer xcodebuild -scheme ClaudeTokenMonitor -destination 'platform=macOS' -project /Users/felixleis/ClaudeCode/ClaudeTokenMonitor/ClaudeTokenMonitor.xcodeproj build 2>&1 | tail -5</automated>
  </verify>
  <done>App builds without errors. DashboardView uses UsageWindowTracker.currentWindow via environment object. PlanUsageBanner no longer has inline computation logic.</done>
</task>

</tasks>

<verification>
- App builds successfully
- PlanUsageBanner uses UsageWindow data model instead of [TokenRecord]
- UsageWindowTracker is injected as environment object in MenubarManager
- No duplicate window computation logic in DashboardView
</verification>

<success_criteria>
- PlanUsageBanner shows remaining tokens from UsageWindowTracker.currentWindow
- No build errors
- Clean separation of concerns: tracker computes, view displays
</success_criteria>

<output>
After completion, create `.planning/quick/260327-1d5-fix-remaining-session-usage-display-and-/260327-1d5-SUMMARY.md`
</output>
