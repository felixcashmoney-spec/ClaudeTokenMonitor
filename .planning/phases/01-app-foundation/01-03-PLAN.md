---
phase: 01-app-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified: []
autonomous: false
requirements:
  - UI-01
  - CONF-03
  - CONF-04

must_haves:
  truths:
    - "App runs on macOS 26 without crashing"
    - "No Dock icon is visible while the app runs"
    - "Menubar icon is visible and clickable"
    - "Clicking the icon opens the NSPopover"
    - "Settings window opens from the popover Settings button"
    - "Login Item toggle in Settings reflects actual SMAppService state"
  artifacts:
    - path: "ClaudeTokenMonitor/ClaudeTokenMonitor/ClaudeTokenMonitorApp.swift"
      provides: "Running app entry point"
      contains: "@main"
    - path: "ClaudeTokenMonitor/ClaudeTokenMonitor/MenubarManager.swift"
      provides: "NSStatusItem + NSPopover"
      contains: "NSPopover"
    - path: "ClaudeTokenMonitor/ClaudeTokenMonitor/LoginItemManager.swift"
      provides: "SMAppService Login Item control"
      contains: "SMAppService"
  key_links:
    - from: "User clicks menubar icon"
      to: "NSPopover opens"
      via: "MenubarManager.togglePopover"
      pattern: "togglePopover"
    - from: "User clicks Settings..."
      to: "Settings window with toggle"
      via: "NSApp.sendAction showSettingsWindow"
      pattern: "showSettingsWindow"
---

<objective>
Human verification of the Phase 1 app foundation. Confirm all four phase success criteria are observable by running the app and exercising each user-facing behavior.

Purpose: Plans 01 and 02 are fully automated. This plan is the final gate before Phase 2 begins — the user confirms what they see matches what was planned.

Output: User approval that the app shell is correct and ready for Phase 2 session tracking.
</objective>

<execution_context>
@/Users/felixleis/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-app-foundation/01-01-SUMMARY.md
@.planning/phases/01-app-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify running app meets all Phase 1 success criteria</name>
  <files></files>
  <action>Human verification only — no code changes. Run the built app and check each success criterion as described in how-to-verify.</action>
  <what-built>
    Plan 01 created the Xcode project with LSUIElement=YES (no Dock icon), an NSStatusItem menubar icon, and an NSPopover containing a placeholder MenubarView.

    Plan 02 added a SwiftData ModelContainer, a LoginItemManager using SMAppService, and a SettingsView with a "Launch at Login" toggle accessible from the popover.
  </what-built>
  <how-to-verify>
    Run the app from Xcode (Product > Run, or Cmd+R) or build and open the .app bundle directly.

    Check each of the four Phase 1 success criteria:

    1. CRASH CHECK
       - App launches. Xcode console should show no uncaught exceptions or crash logs.
       - Expected: App starts silently, no crash dialog.

    2. DOCK ICON CHECK
       - Look at the macOS Dock.
       - Expected: No "ClaudeTokenMonitor" icon appears in the Dock while the app is running.

    3. MENUBAR ICON CHECK
       - Look at the right side of the menubar (status items area).
       - Expected: A chart bar icon (or similar system icon) appears.
       - Click the icon.
       - Expected: A popover appears showing "Claude Token Monitor" heading and placeholder text.
       - Click outside the popover.
       - Expected: Popover closes.

    4. SETTINGS + LOGIN ITEM CHECK
       - Click the menubar icon to open the popover.
       - Click the "Settings..." button in the popover.
       - Expected: A Settings window opens with a "Startup" section containing a "Launch at Login" toggle.
       - Toggle it on.
       - Expected: The toggle flips to ON without error.
       - Open System Settings > General > Login Items.
       - Expected: "ClaudeTokenMonitor" appears in the login items list.
       - Toggle it off in the Settings window.
       - Expected: The entry disappears from System Settings > General > Login Items.
  </how-to-verify>
  <verify>Human confirms all four criteria pass by typing "approved".</verify>
  <done>User has typed "approved" confirming all four Phase 1 success criteria are visually verified on macOS 26.</done>
  <resume-signal>
    Type "approved" if all four checks pass.
    Describe any failures (e.g., "Dock icon still visible", "popover doesn't open", "settings window blank") and I will fix them before proceeding to Phase 2.
  </resume-signal>
</task>

</tasks>

<verification>
All Phase 1 success criteria confirmed by user:
1. App launches without crashing on macOS 26
2. A menubar icon is visible and clicking it opens a popover
3. App is registered as a Login Item and appears in System Settings > General > Login Items when toggled on
4. No Dock icon appears while the app runs
</verification>

<success_criteria>
User types "approved" — confirming all four Phase 1 success criteria are visually verified on their machine running macOS 26.
</success_criteria>

<output>
After user approval, create `.planning/phases/01-app-foundation/01-03-SUMMARY.md` with:
- Status: approved
- All four success criteria: confirmed
- Any issues found and fixed during verification

Then update `.planning/STATE.md`:
- stopped_at: Phase 1 complete
- progress.completed_phases: 1

Next phase: `/gsd:execute-phase 2` (Session Tracking Engine)
</output>
